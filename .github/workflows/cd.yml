name: Continuous Deployment

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # - name: Setup SSH
      #   run: |
      #     mkdir -p ~/.ssh
      #     echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
      #     chmod 600 ~/.ssh/id_rsa
      #     eval $(ssh-agent -s)
      #     ssh-add ~/.ssh/id_rsa
      #     ssh-keyscan -H ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts

      # - name: Deploy to Server
      #   env:
      #     SERVER_HOST: ${{ secrets.SERVER_HOST }}
      #     DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      #   run: |
      #     ssh -o StrictHostKeyChecking=no -i ~/.ssh/id_rsa deploy@${SERVER_HOST} '
      #       set -e
      #       echo "Testing connection..."
      #       whoami
      #       docker info
            
      #       echo "Manipulating containers..."
      #       docker stop cat-dog-app || true
      #       docker rm cat-dog-app || true
            
      #       echo "Pulling new image..."
      #       docker pull '"${DOCKER_USERNAME}"'/cat-dog-app:latest
            
      #       echo "Starting new container..."
      #       docker run -d \
      #         --name cat-dog-app \
      #         -p 5000:5000 \
      #         -p 5001:5001 \
      #         --restart unless-stopped \
      #         '"${DOCKER_USERNAME}"'/cat-dog-app:latest
      #     '

      - name: Decode SSH Key
        run: |
          echo "${{ secrets.FAIZI_PK_BASE64 }}" | base64 --decode > private_key.pem
          chmod 600 private_key.pem
          ls
          cat private_key.pem

      - name: Deploy and Run Docker Container
        uses: appleboy/ssh-action@master
        with:
          host: ec2-3-88-50-111.compute-1.amazonaws.com  # VM IP or hostname
          username: ubuntu  # Usually 'ubuntu', 'ec2-user', or 'root'
          key: ${{ secrets.FAIZI_PK_BASE64 }}  # Private key with access to the VM
          port: 22
          script: |
            docker pull nginx
            